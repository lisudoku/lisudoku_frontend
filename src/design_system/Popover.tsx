import { Popover as MuiPopver, PopoverHandler as MuiPopoverHandler, PopoverContent as MuiPopoverContent } from '@material-tailwind/react/components/Popover'

const Popover = MuiPopver

const PopoverHandler = MuiPopoverHandler

const PopoverContent = MuiPopoverContent

export { Popover, PopoverHandler, PopoverContent }
